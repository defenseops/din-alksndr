services:
  app:
    build:
      context: .
      args:
        VITE_API_URL: ""   # клиент и сервер на одном домене — API URL не нужен
    ports:
      - "3001:3001"
    env_file:
      - ./server/.env
    restart: unless-stopped
